{% extends "layout.njk" %}

{% block content %}

List Of Platforms :
<div class="cards">
  {% for platform in platforms %}
    <div class="card">
      <fieldset>
        <a href="/platform/{{ platform.id }}">
          <img src="{{platform.logo.url}}">
        </a> <br>
        <a href="/platform/{{ platform.id }}">{{ platform.name }}</a>
      </fieldset>
    </div>
  {% endfor %}
</div>
    {% if pageNumber <= 1 %}
    <div class="cards">
      <div class="card">
        <a href="/platformPages?page={{pageNumber-1}}">
          <button class="button" name="page précédente" disabled>
            page précédente
          </button>
        </a>
      </div>
      <div class="card">
        <a href="/platformPages?page={{pageNumber+1}}">
          <button class="button" name="page suivante">
            page suivante
        </button>
        </a>
      </div>
    </div>
    {% else %}
    <div class="cards">
      <div class="card">
        <a href="/platformPages?page={{pageNumber-1}}">
          <button class="button" name="page précédente">
            page précédente
          </button>
        </a>
      </div>
      <div class="card">
        <a href="/platformPages?page={{pageNumber+1}}">
          <button class="button" name="page suivante">
            page suivante
          </button>
        </a>
      </div>
    </div>
    {% endif %}


{% endblock %}
